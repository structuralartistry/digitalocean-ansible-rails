- name: update apt
  action: command /usr/bin/apt-get update
  sudo: True

- name: update cache 
  apt: update_cache=yes
  sudo: True

# Install anything essential that you need here
- name: install essential packages
  apt: pkg=$item state=installed
  sudo: True
  with_items:
    - build-essential
    - curl
    - git
    - python-dev
    - sqlite3
    - python-setuptools
    - python-software-properties
    - libsqlite3-dev

# - name: copy bashrc
#   action: copy src=../templates/bashrc dest=/home/vagrant/.bashrc

